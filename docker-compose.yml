version: '3.7'

services:
  app:
    build: .
    volumes:
      - ./codes:/home/rstudio/codes
      - ./outputs:/home/rstudio/outputs
    environment:
      PASSWORD: passwd
    ports:
      - "80:8787"
